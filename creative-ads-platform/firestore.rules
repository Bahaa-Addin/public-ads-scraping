rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development mode - allow all access
    // In production, restrict based on authentication
    match /{document=**} {
      allow read, write: if true;
    }
  }
}

